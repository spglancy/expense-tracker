<div class="newExpense">
    <span class="expense-content">You spent {{this.value}} for {{this.description}} at {{this.createdAt}} - Category: {{this.category}}</span>
    <form method="POST" action="/home/expenses/{{this._id}}?_method=DELETE">
        <button type="submit" name="button">Delete</button>
    </form>
    <!-- need reciept if there -->
    <!-- add date -->
</div>
<!--need to completely restyle-->
